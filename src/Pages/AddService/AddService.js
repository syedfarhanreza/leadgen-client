import React from 'react';
import useTitle from '../../Hooks/useTitle';

const AddService = () => {
    useTitle('LeadGen-AddService');
    return (
        <div>
            add service
        </div>
    );
};

export default AddService;